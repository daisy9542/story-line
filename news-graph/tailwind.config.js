/** @type {import('tailwindcss').Config} */
module.exports = {
  // ... 其他配置
  theme: {
    extend: {
      // ... 其他扩展
      animation: {
        'bounce-slow': 'bounce 2s infinite',
      },
    },
  },
  // ... 其他配置
}